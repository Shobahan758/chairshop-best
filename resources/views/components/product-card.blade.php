<article class="product-card"><a href="{{route('product',$product)}}" class="product-img"><img src="{{$product->image}}" alt="{{$product->name}}" loading="lazy">@if($product->sale_price)<span class="sale">সাশ্রয়</span>@endif</a><form method="post" action="{{ route('customer.wishlist.add', $product) }}" class="product-wishlist-form">@csrf<button type="submit" class="customer-icon-button" aria-label="উইশলিস্টে যোগ করুন" title="উইশলিস্টে যোগ করুন"><i class="bi bi-heart"></i></button></form><div class="product-body"><small>{{$product->category?->name}}</small><h3><a href="{{route('product',$product)}}">{{$product->name}}</a></h3><div class="stars">★★★★★ <span>(৪.৯)</span></div><div class="d-flex align-items-center justify-content-between"><div class="price">৳{{number_format($product->current_price)}} @if($product->sale_price)<del>৳{{number_format($product->price)}}</del>@endif</div><form method="post" action="{{route('cart.add',$product)}}">@csrf<button class="round-btn" aria-label="কার্টে যোগ করুন"><i class="bi bi-bag-plus"></i></button></form></div></div></article>
